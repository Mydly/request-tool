import { Nav } from '../config/consts';

export default function changeTab(state = { tabBar:"index" }, action) {

    switch (action.type){
        case Nav.ITEM:
        {
            return Object.assign({}, state, {tabBar:action.item});
        }
        default:
            return state;
    }
}