
export default function theme(state = {}, action) {
    switch (action.type){
        case 'setting':
        {
            return Object.assign({}, state, {tabBar:action.item});
        }
        default:
            return state;
    }
}
